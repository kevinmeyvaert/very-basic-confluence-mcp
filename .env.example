# Confluence MCP Server - Environment Variables

# Confluence Base URL (required)
# The base URL of your Confluence instance
# Example: https://your-domain.atlassian.net
CONFLUENCE_BASE_URL=https://your-domain.atlassian.net

# Confluence Username (required)
# Your Confluence username (usually your email)
# Example: user@company.com
CONFLUENCE_USERNAME=your-username

# Confluence API Token (required)
# Create one at: https://id.atlassian.com/manage-profile/security/api-tokens
CONFLUENCE_API_TOKEN=your-api-token

# Default Space Key (required)
# The space key to use when searching content
# Example: TEAM or DOCS
CONFLUENCE_DEFAULT_SPACE_KEY=your-space-key

# Server Configuration (optional)
# The port number to run the server on (default: 3000)
PORT=3000

# The host to bind the server to (default: 0.0.0.0)
# Use 0.0.0.0 to accept connections from any host
# Use localhost or 127.0.0.1 to only accept local connections
HOST=0.0.0.0

# Logging Level (optional)
# Available levels: debug, info, warn, error
# Default: info
LOG_LEVEL=info 